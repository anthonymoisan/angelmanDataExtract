CREATE TABLE T_AngelmanSyndromeConnexion (
    id INT PRIMARY KEY AUTO_INCREMENT,
    gender VARCHAR(255) NOT NULL,
    genotype VARCHAR(255) NOT NULL,
    age INT NOT NULL,
    groupAge VARCHAR(255) NOT NULL,
    country VARCHAR(255) NOT NULL,
    region VARCHAR(255)
);